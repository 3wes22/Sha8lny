#!/bin/bash
# LEGACY: This script previously downloaded Mistral/LLaMA for local inference.
# The active runtime is hosted Gemini (AI_PROVIDER=gemini).
# Local Ollama fallback: install Ollama manually and pull gemma4:e2b if needed.
# This script is retained for reference only and is not part of the demo setup.
echo "See GRADUATION_DEMO_RUNBOOK.md for current demo setup instructions."
